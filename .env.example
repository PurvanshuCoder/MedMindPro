# Backend API root only (no /api). Production: your Express host, not the Vercel frontend URL.
# Use https://… if the server redirects http→https (otherwise POST login can become GET and fail).
VITE_API_BASE_URL=http://localhost:4000

